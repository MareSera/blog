---
/**
 * ProgressBar - 进度条组件
 * 显示播放进度并支持点击/拖动跳转
 */

export interface Props {
  progress: number;      // 0-100
  currentTime: number;   // 秒
  duration: number;      // 秒
  className?: string;
}

const { 
  progress = 0, 
  currentTime = 0, 
  duration = 0,
  className = '' 
} = Astro.props;
---

<div class={`progress-bar-container ${className}`}>
  <div class="progress-bar-wrapper">
    <div class="progress-bar-track">
      <div 
        class="progress-bar-fill" 
        style={`width: ${Math.min(100, Math.max(0, progress))}%`}
      >
        <div class="progress-bar-thumb"></div>
      </div>
    </div>
  </div>
  
  <div class="progress-bar-time">
    <span class="time-current" data-time={currentTime}></span>
    <span class="time-separator">/</span>
    <span class="time-duration" data-time={duration}></span>
  </div>
</div>

<script>
  import { formatTime } from './utils';
  
  // 格式化并显示时间
  function updateTimeDisplay() {
    document.querySelectorAll('.time-current').forEach(el => {
      const time = parseFloat(el.getAttribute('data-time') || '0');
      el.textContent = formatTime(time);
    });
    
    document.querySelectorAll('.time-duration').forEach(el => {
      const time = parseFloat(el.getAttribute('data-time') || '0');
      el.textContent = formatTime(time);
    });
  }
  
  // 初始化时间显示
  updateTimeDisplay();
  
  // 监听属性变化
  const observer = new MutationObserver(updateTimeDisplay);
  document.querySelectorAll('.time-current, .time-duration').forEach(el => {
    observer.observe(el, { attributes: true, attributeFilter: ['data-time'] });
  });
</script>

<style>
  .progress-bar-container {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    width: 100%;
  }
  
  .progress-bar-wrapper {
    position: relative;
    width: 100%;
    padding: 0.25rem 0;
    cursor: pointer;
    transition: padding 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .progress-bar-wrapper:hover {
    padding: 0.375rem 0;
  }
  
  .progress-bar-track {
    position: relative;
    width: 100%;
    height: 3px;
    background: oklch(var(--bc) / 0.08);
    border-radius: 1.5px;
    overflow: visible;
    transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .progress-bar-wrapper:hover .progress-bar-track {
    height: 4px;
    background: oklch(var(--bc) / 0.1);
  }
  
  .progress-bar-fill {
    position: relative;
    height: 100%;
    background: linear-gradient(90deg, oklch(var(--bc) / 0.8) 0%, oklch(var(--bc) / 0.6) 100%);
    border-radius: 1.5px;
    transition: width 0.1s linear;
    box-shadow: 0 0 10px oklch(var(--bc) / 0.2);
  }

  .progress-bar-fill::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent 0%, oklch(var(--bc) / 0.4) 100%);
    border-radius: 1.5px;
    animation: shimmer 2.5s ease-in-out infinite;
  }

  @keyframes shimmer {
    0%, 100% {
      opacity: 0;
    }
    50% {
      opacity: 1;
    }
  }
  
  .progress-bar-thumb {
    position: absolute;
    right: -5px;
    top: 50%;
    transform: translateY(-50%) scale(0);
    width: 10px;
    height: 10px;
    background: oklch(var(--bc));
    border-radius: 50%;
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    box-shadow: 0 2px 8px oklch(var(--bc) / 0.3), 0 0 0 3px oklch(var(--bc) / 0.2);
  }
  
  .progress-bar-wrapper:hover .progress-bar-thumb {
    transform: translateY(-50%) scale(1);
  }

  .progress-bar-wrapper:active .progress-bar-thumb {
    transform: translateY(-50%) scale(1.3);
  }
  
  .progress-bar-time {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.625rem;
    color: oklch(var(--bc) / 0.45);
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.02em;
    font-weight: 500;
  }
  
  .time-separator {
    margin: 0 0.25rem;
    opacity: 0.4;
  }
  
  /* 响应式设计 */
  @media (max-width: 640px) {
    .progress-bar-time {
      font-size: 0.5625rem;
    }

    .progress-bar-track {
      height: 3px;
    }

    .progress-bar-wrapper:hover .progress-bar-track {
      height: 4px;
    }
  }
</style>
